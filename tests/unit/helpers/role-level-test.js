import { roleLevel } from '../../../helpers/role-level';
import { module, test } from 'qunit';

module('Unit | Helper | role level');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = roleLevel(42);
  assert.ok(result);
});
